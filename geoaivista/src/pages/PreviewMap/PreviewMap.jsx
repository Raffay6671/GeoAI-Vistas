/** @format */

// pages/PreviewMap/PreviewMap.jsx
import React from "react";
import styles from "../../styles/PreviewMap.module.css";

const PreviewMap = () => {
	return (
		<div className={styles.previewMap}>
			<h1>Preview Map Page</h1>
			<p>This is where the preview of the uploaded map will be displayed.</p>
		</div>
	);
};

export default PreviewMap;
